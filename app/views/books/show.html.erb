<!--書籍の詳細画面（Book Detail）-->

<div>
	<!--フラッシュメッセージの表示-->
	<%= flash[:notice] %>
</div>

<div class='container px-5 px-sm-0'>
	<div class='row'>

		<!--画面左側-->
		<!--テンプレートの呼び出し（render '部分テンプレートファイル名', ローカル変数:渡す値）-->
		<%= render partial:'newform', locals:{user:@user, book:@book_new} %>
		<!--使用する変数を複数定義するときは、render partial: '部分テンプレートファイル名', locals: { ローカル変数1:渡す値1, ローカル変数2:渡す値2, ローカル変数3:渡す値3 }-->


    <!--画面右側-->
    <!--書籍の詳細-->
    <div class='col-md-8 offset-md-1'>
      <h2>Book Detail</h2>
      <table class='table table-hover table-inverse'>
        <tbody>
          <tr>
            <!--ユーザー画像、ユーザー名-->
            <td><%= image_tag @user.get_profile_image(100,100) %><br>
                <!--ユーザーの詳細画面(users/show.html.erb)へ遷移-->
                <%= link_to @user.name, user_path(@user.id) %></td>
            <!--書籍詳細画面(books/show.html.erb)へ遷移-->
            <td><%= link_to @book.title, book_path %></td>
            <td><%= @book.body %></td>
            <!--編集ボタン　書籍編集ページ(books/edit.html.erb)へ遷移-->
            <td><%= link_to 'Edit', edit_book_path(@book) %></td>
            <!--削除ボタン　destroyはHTTPメソッドがPOSTでなくDELETEのため、HTTPmメソッドをdeleteに指定-->
            <td><%= link_to 'Destroy', book_path(@book), method: :delete, "data-confirm" => "本当に消しますか？" %></td>
          </tr>
        </tbody>
      </table>
    </div>

  </div>
</div>
